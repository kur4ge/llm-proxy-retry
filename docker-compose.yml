services:
  llm-proxy:
    image: llm-proxy-retry:local
    build:
      context: .
      dockerfile: Dockerfile
    command: ["-config", "/config.yaml"]
    ports:
      - "${LLM_PROXY_PORT:-8080}:8080"
    volumes:
      - ./config.yaml:/config.yaml:ro
    restart: unless-stopped
